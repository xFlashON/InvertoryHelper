<?xml version="1.0" encoding="utf-8"?>

<ContentPage
    x:Class="InvertoryHelper.View.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:InvertoryHelper.View"
    Title="Invertory Helper">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="BorderRadius" Value="10" />
                <Setter Property="BackgroundColor" Value="DarkTurquoise" />
            </Style>
        </ResourceDictionary>

    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" />
        <ToolbarItem Order="Secondary" Text="Settings" />
        <ToolbarItem
            Command="{Binding OpenRefereccesPage}"
            Order="Secondary"
            Text="References" />
    </ContentPage.ToolbarItems>

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Text="Orders" />
        <Button Grid.Row="1" Text="Recounts" />
        <Button Grid.Row="2" Text="Price checking" />

    </Grid>

</ContentPage>